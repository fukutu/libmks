diff --git a/lib/mks-clipboard-redirector.c b/lib/mks-clipboard-redirector.c
index b6331b7..0000000 100644
--- a/lib/mks-clipboard-redirector.c
+++ b/lib/mks-clipboard-redirector.c
@@ -27,13 +27,21 @@ typedef struct
   MksClipboardRedirector *redirector;
   MksClipboardSelection   selection;
   DexPromise             *promise;
+  GInputStream           *input;
   GOutputStream          *stream;
   char                   *mime_type;
 } ReadState;
 
+typedef struct
+{
+  GOutputStream *stream;
+  GBytes        *bytes;
+} RemoteWriteState;
+
 static void mks_remote_content_provider_set_property (GObject      *object,
                                                       guint         prop_id,
                                                       const GValue *value,
@@ -86,21 +94,39 @@ read_state_free (ReadState *state)
 {
   g_clear_object (&state->redirector);
   dex_clear (&state->promise);
+  g_clear_object (&state->input);
   g_clear_object (&state->stream);
   g_clear_pointer (&state->mime_type, g_free);
   g_free (state);
 }
 
 static void
-mks_clipboard_redirector_read_cb (GObject      *object,
-                                  GAsyncResult *result,
-                                  gpointer      user_data)
+remote_write_state_free (RemoteWriteState *state)
 {
-  GdkContentProvider *provider = GDK_CONTENT_PROVIDER (object);
+  g_clear_object (&state->stream);
+  g_clear_pointer (&state->bytes, g_bytes_unref);
+  g_free (state);
+}
+
+static void
+mks_clipboard_redirector_splice_cb (GObject      *object,
+                                    GAsyncResult *result,
+                                    gpointer      user_data)
+{
+  GOutputStream *stream = G_OUTPUT_STREAM (object);
   ReadState *state = user_data;
   g_autoptr(GBytes) bytes = NULL;
   g_autoptr(MksClipboardContent) content = NULL;
   g_autoptr(GError) error = NULL;
 
-  g_assert (GDK_IS_CONTENT_PROVIDER (provider));
+  g_assert (G_IS_OUTPUT_STREAM (stream));
   g_assert (state != NULL);
 
-  if (!gdk_content_provider_write_mime_type_finish (provider, result, &error))
+  if (g_output_stream_splice_finish (stream, result, &error) < 0)
     {
       dex_promise_reject (state->promise, g_steal_pointer (&error));
       read_state_free (state);
@@ -123,6 +149,49 @@ mks_clipboard_redirector_read_cb (GObject      *object,
   read_state_free (state);
 }
 
+static void
+mks_clipboard_redirector_read_cb (GObject      *object,
+                                  GAsyncResult *result,
+                                  gpointer      user_data)
+{
+  GdkClipboard *clipboard = GDK_CLIPBOARD (object);
+  ReadState *state = user_data;
+  g_autoptr(GInputStream) input = NULL;
+  g_autoptr(GError) error = NULL;
+  const char *mime_type = NULL;
+
+  g_assert (GDK_IS_CLIPBOARD (clipboard));
+  g_assert (state != NULL);
+
+  if (!(input = gdk_clipboard_read_finish (clipboard,
+                                           result,
+                                           &mime_type,
+                                           &error)))
+    {
+      dex_promise_reject (state->promise, g_steal_pointer (&error));
+      read_state_free (state);
+      return;
+    }
+
+  if (mime_type == NULL)
+    {
+      error = g_error_new_literal (G_IO_ERROR,
+                                   G_IO_ERROR_NOT_SUPPORTED,
+                                   "Clipboard content type is not available");
+      dex_promise_reject (state->promise, g_steal_pointer (&error));
+      read_state_free (state);
+      return;
+    }
+
+  state->input = g_steal_pointer (&input);
+  state->mime_type = g_strdup (mime_type);
+
+  g_output_stream_splice_async (state->stream,
+                                state->input,
+                                G_OUTPUT_STREAM_SPLICE_CLOSE_SOURCE,
+                                G_PRIORITY_DEFAULT,
+                                dex_promise_get_cancellable (state->promise),
+                                mks_clipboard_redirector_splice_cb,
+                                state);
+}
+
 static MksClipboardSelection
 selection_from_clipboard (MksClipboardRedirector *self,
                           GdkClipboard           *clipboard)
@@ -259,11 +328,7 @@ mks_clipboard_redirector_read (MksClipboard          *clipboard,
                                gpointer               user_data)
 {
   MksClipboardRedirector *self = user_data;
-  g_autoptr(GdkContentFormats) formats = NULL;
   GdkClipboard *gdk_clipboard;
-  GdkContentProvider *provider;
-  GdkContentFormats *provider_formats;
-  const char *mime_type;
   ReadState *state;
 
   g_assert (MKS_IS_CLIPBOARD (clipboard));
@@ -275,41 +340,19 @@ mks_clipboard_redirector_read (MksClipboard          *clipboard,
                                   "Clipboard redirection is disabled");
 
   gdk_clipboard = clipboard_from_selection (self, selection);
-  provider = gdk_clipboard_get_content (gdk_clipboard);
-
-  if (provider == NULL)
-    return dex_future_new_reject (G_IO_ERROR,
-                                  G_IO_ERROR_NOT_FOUND,
-                                  "Clipboard content is not available");
-
-  formats = gdk_content_formats_new ((const char **) mime_types,
-                                     g_strv_length ((char **) mime_types));
-  provider_formats = gdk_content_provider_ref_formats (provider);
-  mime_type = gdk_content_formats_match_mime_type (formats, provider_formats);
-  gdk_content_formats_unref (provider_formats);
-
-  if (mime_type == NULL)
-    return dex_future_new_reject (G_IO_ERROR,
-                                  G_IO_ERROR_NOT_SUPPORTED,
-                                  "Clipboard content type is not available");
 
   state = g_new0 (ReadState, 1);
   state->redirector = g_object_ref (self);
   state->selection = selection;
   state->promise = dex_promise_new_cancellable ();
   state->stream = g_memory_output_stream_new_resizable ();
-  state->mime_type = g_strdup (mime_type);
 
-  gdk_content_provider_write_mime_type_async (provider,
-                                              mime_type,
-                                              state->stream,
-                                              G_PRIORITY_DEFAULT,
-                                              dex_promise_get_cancellable (state->promise),
-                                              mks_clipboard_redirector_read_cb,
-                                              state);
+  gdk_clipboard_read_async (gdk_clipboard,
+                            (const char **) mime_types,
+                            G_PRIORITY_DEFAULT,
+                            dex_promise_get_cancellable (state->promise),
+                            mks_clipboard_redirector_read_cb,
+                            state);
 
   return DEX_FUTURE (dex_ref (state->promise));
 }
@@ -573,9 +616,9 @@ mks_remote_content_provider_request_cb (GObject      *object,
 {
   MksClipboard *clipboard = MKS_CLIPBOARD (object);
   g_autoptr(GTask) task = user_data;
+  RemoteWriteState *state;
   g_autoptr(MksClipboardContent) content = NULL;
-  g_autoptr(GBytes) bytes = NULL;
   g_autoptr(GError) error = NULL;
   GOutputStream *stream;
   gconstpointer data;
@@ -592,9 +635,12 @@ mks_remote_content_provider_request_cb (GObject      *object,
       return;
     }
 
-  stream = g_task_get_task_data (task);
-  bytes = mks_clipboard_content_dup_bytes (content);
-  data = g_bytes_get_data (bytes, &size);
+  state = g_task_get_task_data (task);
+  g_assert (state != NULL);
+
+  stream = state->stream;
+  state->bytes = mks_clipboard_content_dup_bytes (content);
+  data = g_bytes_get_data (state->bytes, &size);
 
+  /* write_all_async() does not copy @data, so keep @bytes alive in @task. */
   g_output_stream_write_all_async (stream,
                                    data,
                                    size,
@@ -611,6 +657,7 @@ mks_remote_content_provider_write_mime_type_async (GdkContentProvider  *provider,
                                                    gpointer             user_data)
 {
   MksRemoteContentProvider *self = MKS_REMOTE_CONTENT_PROVIDER (provider);
+  RemoteWriteState *state;
   g_autoptr(GTask) task = NULL;
   const char *mime_types[] = { NULL, NULL };
 
@@ -620,7 +667,11 @@ mks_remote_content_provider_write_mime_type_async (GdkContentProvider  *provider,
 
   task = g_task_new (provider, cancellable, callback, user_data);
   g_task_set_priority (task, io_priority);
-  g_task_set_task_data (task, g_object_ref (stream), g_object_unref);
+
+  state = g_new0 (RemoteWriteState, 1);
+  state->stream = g_object_ref (stream);
+  g_task_set_task_data (task, state, (GDestroyNotify) remote_write_state_free);
 
   mime_types[0] = mime_type;
 
